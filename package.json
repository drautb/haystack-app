{
  "name": "haystack-app",
  "description": "A web frontend for haystack",
  "version": "1.0.0",
  "homepage": "https://github.com/drautb/haystack-app",
  "repository": "https://github.com/drautb/haystack-app",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000",

    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
  }
}